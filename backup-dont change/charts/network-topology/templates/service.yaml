apiVersion: v1
kind: Service
metadata:
  name: {{ include "network-topology.fullname" . }}
  labels:
    {{- include "network-topology.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "network-topology.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
