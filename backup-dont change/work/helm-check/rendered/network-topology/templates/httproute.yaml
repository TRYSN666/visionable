---
# Source: network-topology/templates/httproute.yaml
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: topology-network-topology
  labels:
    helm.sh/chart: network-topology-0.1.0
    app.kubernetes.io/name: network-topology
    app.kubernetes.io/instance: topology
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: internal-gateway
  hostnames:
    - topology.internal.example
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: "/"
      backendRefs:
        - name: topology-network-topology
          port: 80

